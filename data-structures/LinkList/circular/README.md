# Circular Linked List

## 🎯 設計理念

### 存在意義

**這個工具被發明出來，是為了解決什麼類型的問題？**

**痛點**：標準單向/雙向 Linked List 在處理「循環結構」時有以下問題：

1. **遍歷終止判斷**：必須額外記錄起點或使用 `null` 判斷，無法自然地「繞回來」
2. **輪詢機制**：Round-Robin 等需要循環存取的場景，用一般 List 需要手動「回到頭部」
3. **環狀關係建模**：撲克牌發牌、約瑟夫問題等天然環狀結構，用線性結構表達不直觀

**價值主張**：將「尾部」連回「頭部」，使遍歷形成閉環，自然支援循環存取

**一句話本質**：專門處理「需要不斷繞圈」或「沒有明確終點」的序列結構

```plaintext
【對比】
一般 Linked List：head → A → B → C → null（有終點）
Circular Linked List：head → A → B → C → head（無終點，繞回起點）
```

### 心智模型

**類比：圓桌會議**

想像一個圓桌，每個人代表一個節點：

- **next 指標**：你右手邊的人是誰
- **遍歷**：從任何人開始，沿著「右手邊」一直走，最終會回到起點
- **插入**：有新人加入，坐到某人旁邊，調整「右手邊是誰」的指向
- **刪除**：有人離開，他左右兩邊的人重新牽手

**局限性**：

- 圓桌上的人有視覺上的「位置」，但 Circular List 沒有——所有操作都依賴指標追蹤
- 圓桌可以「跳過」人直接喊，但 Circular List 必須逐一遍歷

---

## 🏗️ 抽象結構

### 核心組件

| 組件       | 功能                      | 為什麼需要                                            |
| ---------- | ------------------------- | ----------------------------------------------------- |
| **Node**   | 儲存資料 + 指向下一個節點 | 基本儲存單元                                          |
| **tail**   | 指向最後一個節點          | 透過 `tail.next` 可 O(1) 存取 head；插入尾部也是 O(1) |
| **length** | 記錄節點數量              | O(1) 取得大小，避免遍歷計數；**判斷遍歷終止**         |

> ⚠️ **關鍵設計決策**：為什麼用 `tail` 而不是 `head`？
>
> 若只存 `head`：
>
> - 存取頭部：O(1) ✓
> - 存取尾部：O(n) ✗（需要遍歷整圈）
> - 尾部插入：O(n) ✗
>
> 若只存 `tail`：
>
> - 存取頭部：O(1) ✓（`tail.next` 就是 head）
> - 存取尾部：O(1) ✓
> - 尾部插入：O(1) ✓
>
> **結論**：存 `tail` 嚴格優於存 `head`

### 狀態表示

```javascript
class Node {
  constructor(value) {
    this.value = value;
    this.next = null;
  }
}

class CircularLinkedList {
  constructor() {
    this.tail = null; // 指向最後一個節點
    this.length = 0; // 節點數量
  }

  // head 透過 getter 動態計算
  get head() {
    return this.tail ? this.tail.next : null;
  }
}
```

**初始狀態**：`tail = null`, `length = 0`

**單一節點狀態**：節點的 `next` 指向自己

```plaintext
tail → [A] ─┐
       ↑    │
       └────┘
```

**多節點狀態**：

```plaintext
         ┌──────────────────────┐
         ↓                      │
head → [A] → [B] → [C] → [D] ──┘
                          ↑
                         tail
```

### 核心操作

#### 1. append(value) - 尾部插入

**目的**：在尾部新增節點

**輸入/輸出**：`value` → 無回傳（或回傳 `this` 支援鏈式呼叫）

**不變量**：

- `tail` 永遠指向最後一個節點
- `tail.next` 永遠指向 head
- `length` 正確反映節點數

**基礎實作**：

```javascript
append(value) {
  const newNode = new Node(value);

  if (!this.tail) {
    // Case 1: 空 list，新節點指向自己
    newNode.next = newNode;
    this.tail = newNode;
  } else {
    // Case 2: 非空 list
    newNode.next = this.tail.next;  // 新節點指向 head
    this.tail.next = newNode;        // 舊 tail 指向新節點
    this.tail = newNode;             // 更新 tail
  }

  this.length++;
}
```

**視覺化說明**：

```plaintext
【初始狀態】
         ┌─────────────┐
         ↓             │
head → [A] → [B] → [C]─┘
                    ↑
                   tail

【插入 D】
Step 1: newNode.next = tail.next (D → A)
Step 2: tail.next = newNode (C → D)
Step 3: tail = newNode

         ┌──────────────────┐
         ↓                  │
head → [A] → [B] → [C] → [D]┘
                          ↑
                         tail
```

**複雜度**：Time O(1), Space O(1)

---

#### 2. prepend(value) - 頭部插入

**目的**：在頭部新增節點

**基礎實作**：

```javascript
prepend(value) {
  const newNode = new Node(value);

  if (!this.tail) {
    newNode.next = newNode;
    this.tail = newNode;
  } else {
    newNode.next = this.tail.next;  // 新節點指向舊 head
    this.tail.next = newNode;        // tail 指向新 head
    // tail 不變！
  }

  this.length++;
}
```

**關鍵差異**：與 `append` 幾乎相同，唯一差別是 **不更新 tail**

**複雜度**：Time O(1), Space O(1)

---

#### 3. traverse() - 遍歷

**目的**：存取所有節點

**基礎實作的問題**：如何知道「繞完一圈」了？

```javascript
// ❌ 錯誤：無限迴圈
traverse() {
  let current = this.head;
  while (current) {  // current 永遠不會是 null！
    console.log(current.value);
    current = current.next;
  }
}
```

**正確實作 - 方法 A：計數法**

```javascript
traverse() {
  if (!this.tail) return [];

  const result = [];
  let current = this.head;

  for (let i = 0; i < this.length; i++) {
    result.push(current.value);
    current = current.next;
  }

  return result;
}
```

**正確實作 - 方法 B：起點比對法**

```javascript
traverse() {
  if (!this.tail) return [];

  const result = [];
  let current = this.head;

  do {
    result.push(current.value);
    current = current.next;
  } while (current !== this.head);  // 回到起點就停

  return result;
}
```

**兩種方法比較**：

| 方法       | 優點                   | 缺點                   |
| ---------- | ---------------------- | ---------------------- |
| 計數法     | 簡單直觀，不怕指標錯誤 | 需要維護 length 變數   |
| 起點比對法 | 不需要 length          | 如果指標壞了會無限迴圈 |

**複雜度**：Time O(n), Space O(1) 或 O(n) 視是否收集結果

---

#### 4. delete(value) - 刪除節點

**目的**：移除特定值的節點

**這是 Circular List 最複雜的操作，需要處理多種情況**

```javascript
delete(value) {
  if (!this.tail) return false;

  let current = this.head;
  let prev = this.tail;  // 頭部的前一個就是 tail

  // 遍歷尋找目標
  for (let i = 0; i < this.length; i++) {
    if (current.value === value) {
      // 找到了，分情況處理

      if (this.length === 1) {
        // Case 1: 只有一個節點
        this.tail = null;
      } else if (current === this.head) {
        // Case 2: 刪除頭部
        this.tail.next = current.next;
      } else if (current === this.tail) {
        // Case 3: 刪除尾部
        prev.next = this.head;
        this.tail = prev;
      } else {
        // Case 4: 刪除中間
        prev.next = current.next;
      }

      this.length--;
      return true;
    }

    prev = current;
    current = current.next;
  }

  return false;  // 找不到
}
```

**視覺化 - Case 3（刪除尾部）**：

```plaintext
【刪除前】刪除 C
         ┌─────────────┐
         ↓             │
head → [A] → [B] → [C]─┘
        ↑           ↑
       prev       current/tail

【刪除後】
         ┌────────┐
         ↓        │
head → [A] → [B]──┘
              ↑
             tail
```

**複雜度**：Time O(n), Space O(1)

---

### 核心操作複雜度總結

| 操作           | 時間複雜度 | 空間複雜度 | 備註          |
| -------------- | ---------- | ---------- | ------------- |
| **存取**       |
| 存取頭部       | O(1)       | O(1)       | `tail.next`   |
| 存取尾部       | O(1)       | O(1)       | `tail`        |
| 存取第 i 個    | O(n)       | O(1)       | 需要遍歷      |
| **插入**       |
| prepend (頭部) | **O(1)**   | O(1)       | 不需要遍歷    |
| append (尾部)  | **O(1)**   | O(1)       | 直接操作 tail |
| insert (中間)  | O(n)       | O(1)       | 需要找到位置  |
| **刪除**       |
| delete (頭部)  | O(1)       | O(1)       | 直接操作      |
| delete (尾部)  | O(n)       | O(1)       | 需要找到 prev |
| delete (中間)  | O(n)       | O(1)       | 需要找到位置  |
| **搜尋**       |
| find(value)    | O(n)       | O(1)       | 線性搜尋      |
| **遍歷**       |
| traverse       | O(n)       | O(1)       | 需要終止條件  |

**符號定義**：

- `n`：節點數量（length）

**結構本身的空間複雜度**：O(n)

---

## ⭐ 抽象化翻譯器

### 識別核心抽象

> **Circular Linked List 的核心對象是**：節點序列
>
> **它管理的是這些對象之間的什麼關係？** 有序的環狀連接關係，最後一個元素連回第一個

### 建立映射維度

| 維度                 | 要回答的問題                             | 這個答案決定了什麼     |
| -------------------- | ---------------------------------------- | ---------------------- |
| **維度 1：循環性**   | 問題是否涉及「繞回起點」或「無限循環」？ | 是否需要 Circular 結構 |
| **維度 2：存取模式** | 主要從哪裡存取？頭/尾/任意位置？         | 選擇存 head 還是 tail  |
| **維度 3：終止條件** | 如何判斷「完成一輪」？                   | 用計數還是起點比對     |

### 實戰檢查表

```plaintext
題目：_______________

維度 1 - 循環性：問題是否需要「繞回起點」？
□ 是 → 適合 Circular Linked List
□ 否 → 考慮一般 Linked List

維度 2 - 存取模式：
□ 頻繁存取頭尾 → 存 tail 指標
□ 只需存取頭部 → 存 head 也可

維度 3 - 終止條件：
□ 已知總數 → 用計數法
□ 未知總數 → 用起點比對法
```

### 映射範例

**題目 A**：[LeetCode 622. Design Circular Queue](https://leetcode.com/problems/design-circular-queue/)

| 維度   | 具體問題                   | 抽象映射                     |
| ------ | -------------------------- | ---------------------------- |
| 維度 1 | Queue 滿了要繞回開頭覆蓋   | ✓ 需要 Circular 結構         |
| 維度 2 | enqueue 在尾、dequeue 在頭 | 需要 O(1) 存取兩端 → 存 tail |
| 維度 3 | 固定容量 k                 | 用計數法判斷滿/空            |

**題目 B**：[約瑟夫問題 (Josephus Problem)](https://leetcode.com/problems/find-the-winner-of-the-circular-game/)

| 維度   | 具體問題                    | 抽象映射                           |
| ------ | --------------------------- | ---------------------------------- |
| 維度 1 | n 個人圍成圈，數到 k 就淘汰 | ✓ 天然環狀結構                     |
| 維度 2 | 從任意位置開始，逐一移動    | 不需要 O(1) 存取兩端               |
| 維度 3 | 每輪淘汰一人直到剩一人      | length 會變，用起點比對 + 計數混合 |

---

## 🔍 觸發器（模式識別）

### 正向觸發器

看到什麼特徵應該想到 Circular Linked List？

| 層級     | 特徵                                            | 為什麼這個特徵指向這個工具 |
| -------- | ----------------------------------------------- | -------------------------- |
| 關鍵字   | 「圓形」「環狀」「循環」「輪詢」「Round-Robin」 | 直接描述環狀結構           |
| 結構     | 最後一個元素需要連回第一個                      | Circular 的定義            |
| 目標     | 需要「繞圈」遍歷、不斷循環存取                  | 一般 List 無法自然實現     |
| 操作模式 | 頻繁在頭尾兩端操作                              | tail 指標讓兩端都是 O(1)   |

**一句話觸發規則**：看到「環狀結構」或「需要繞回起點」→ 想到 Circular Linked List

### 反向觸發器

什麼情況下「看起來適用但其實不是」？

| 陷阱情境           | 為什麼不適用                       | 更好的選擇             |
| ------------------ | ---------------------------------- | ---------------------- |
| 需要 O(1) 隨機存取 | Circular List 存取第 i 個仍是 O(n) | Array / Circular Array |
| 只需要 FIFO        | 不需要環的複雜性                   | 一般 Queue             |
| 頻繁在中間插入刪除 | 仍需 O(n) 找位置                   | 視情況選擇其他結構     |
| 環的檢測問題       | 是「判斷」有沒有環，不是「使用」環 | 快慢指標 (Floyd's)     |

### 與類似工具的決策點

```plaintext
【線性序列 + 循環需求】
├── 需要 O(1) 隨機存取 + 固定大小 → Circular Array
├── 需要 O(1) 頭尾插入 + 動態大小 → Circular Linked List
├── 只需要 FIFO → 一般 Queue
└── 只需要 LIFO → 一般 Stack

【環相關問題】
├── 「使用」環狀結構儲存資料 → Circular Linked List
├── 「檢測」是否存在環 → 快慢指標 (Floyd's Cycle Detection)
└── 「找」環的入口點 → 快慢指標 + 數學推導
```

---

## ⚠️ 注意事項

### 常見陷阱

| 陷阱               | 錯誤寫法                   | 正確寫法                                                              | 為什麼                         |
| ------------------ | -------------------------- | --------------------------------------------------------------------- | ------------------------------ |
| 無限迴圈           | `while (current !== null)` | `for (let i = 0; i < length; i++)` 或 `do...while (current !== head)` | Circular List 永遠沒有 null    |
| 空 List 存取       | `this.tail.next` 直接存取  | 先檢查 `if (!this.tail)`                                              | tail 可能是 null               |
| 單節點處理         | 假設 head ≠ tail           | 檢查 `length === 1` 或 `tail.next === tail`                           | 單節點時 head 和 tail 是同一個 |
| 刪除後 tail 未更新 | 刪除 tail 後忘記更新       | `this.tail = prev`                                                    | tail 指向被刪除的節點會出錯    |

### 常見變體

| 變體                             | 修改內容                | 適用場景                        |
| -------------------------------- | ----------------------- | ------------------------------- |
| **Circular Doubly Linked List**  | 每個節點有 prev 和 next | 需要雙向遍歷、O(1) 刪除已知節點 |
| **Circular Array**               | 用 Array 實現環狀結構   | 固定大小、需要 O(1) 隨機存取    |
| **帶 sentinel 的 Circular List** | 加入虛擬節點簡化邊界    | 減少空 List 的特殊處理          |

---

## 🔗 相關題目練習

| 題號 | 題目                                      | 關聯性                 | 難度   |
| ---- | ----------------------------------------- | ---------------------- | ------ |
| 622  | Design Circular Queue                     | 直接應用 Circular 結構 | Medium |
| 641  | Design Circular Deque                     | 雙端版本               | Medium |
| 1823 | Find the Winner of the Circular Game      | 約瑟夫問題             | Medium |
| 708  | Insert into a Sorted Circular Linked List | 有序環狀插入           | Medium |
