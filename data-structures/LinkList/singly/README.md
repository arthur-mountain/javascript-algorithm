# Singly Linked List

## 🎯 設計理念

### 存在意義

**這個工具被發明出來，是為了解決什麼類型的問題？**

| 面向           | 說明                                                                                |
| -------------- | ----------------------------------------------------------------------------------- |
| **痛點**       | Array 在中間插入/刪除需要 O(n) 移動元素；靜態 Array 需要預先知道大小，擴容代價高昂  |
| **價值主張**   | 以 O(1) 完成任意位置的插入/刪除（前提：已持有該位置的指標），且不需要連續記憶體空間 |
| **一句話本質** | **用「指標連接」換取「位置自由」的線性資料結構**                                    |

**關鍵 trade-off**：

```plaintext
Array：連續記憶體 → O(1) 隨機存取 → O(n) 插入/刪除
Linked List：分散記憶體 → O(n) 隨機存取 → O(1) 插入/刪除*
                                            *（已知位置時）
```

### 心智模型

**類比：火車車廂**

| 操作      | 火車類比                               | Linked List 對應         |
| --------- | -------------------------------------- | ------------------------ |
| 節點      | 一節車廂                               | `{ value, next }`        |
| next 指標 | 車廂之間的連結器                       | 指向下一個節點的參考     |
| head      | 火車頭                                 | 第一個節點的參考         |
| tail      | 火車尾                                 | 最後一個節點的參考       |
| 遍歷      | 從車頭走到車尾，每節車廂只能看到下一節 | `current = current.next` |
| 插入      | 解開連結器，插入新車廂，重新連接       | 修改 next 指標           |

**局限**：

- 真實火車可以倒退回去看前面的車廂；Singly Linked List **不行**（沒有 prev 指標）
- 火車乘客可以直接跳到第 N 節車廂；Linked List **必須從頭走過去**

---

## 🏗️ 抽象結構

### 核心組件

| 組件       | 功能                         | 為什麼需要                            |
| ---------- | ---------------------------- | ------------------------------------- |
| **Node**   | 存儲資料和指向下一節點的指標 | 基本單位，實現「分散存儲 + 指標連接」 |
| **head**   | 指向第一個節點               | 進入點，沒有它無法存取任何節點        |
| **tail**   | 指向最後一個節點             | 優化尾部操作從 O(n) → O(1)            |
| **length** | 記錄節點數量                 | 避免每次都要遍歷才知道大小            |

### 狀態表示

```javascript
// Node 結構
{ value: any, next: Node | null }

// LinkedList 狀態
{
  head: Node | null,  // 空串列時為 null
  tail: Node | null,  // 空串列時為 null
  length: number      // 初始為 0
}
```

**關鍵不變量（Invariants）**：

1. `length === 0` ⟺ `head === null && tail === null`
2. `length === 1` ⟺ `head === tail && head !== null`
3. `tail.next === null`（永遠成立，除非是 Circular Linked List）
4. 從 head 出發，經過 `length - 1` 次 `.next` 必定到達 tail

---

### 核心操作

#### 1. **get(index) — 隨機存取**

**目的**：取得第 index 個節點

**輸入/輸出**：`index: number` → `Node | null`

**不變量**：不修改任何節點

**基礎實作**：

```javascript
get(index) {
  if (index < 0 || index >= this.length) return null;

  let current = this.head;
  for (let i = 0; i < index; i++) {
    current = current.next;
  }
  return current;
}
```

**複雜度**：O(n) — 這是 Linked List 相對於 Array 的**根本劣勢**

**為什麼無法優化**：

- 沒有連續記憶體 → 無法用 `base + offset * size` 直接計算地址
- 必須沿著 next 指標逐一前進

---

#### 2. **prepend(value) — 頭部插入**

**目的**：在串列開頭插入新節點

**輸入/輸出**：`value: any` → `this`

**不變量**：插入後 `head` 指向新節點

**實作**：

```javascript
prepend(value) {
  const node = new ListNode(value);

  if (!this.head) {
    // 空串列：head 和 tail 都指向新節點
    this.head = this.tail = node;
  } else {
    // 非空：新節點指向原 head，更新 head
    node.next = this.head;
    this.head = node;
  }

  this.length++;
  return this;
}
```

**視覺化**：

```plaintext
Before: head → [A] → [B] → [C] → null

Insert X at head:

Step 1: [X].next = head
        [X] → [A] → [B] → [C] → null
              ↑
             head

Step 2: head = [X]
        [X] → [A] → [B] → [C] → null
        ↑
       head
```

**複雜度**：O(1) — 這是 Linked List 相對於 Array 的**核心優勢**

---

#### 3. **append(value) — 尾部插入**

**目的**：在串列結尾插入新節點

**輸入/輸出**：`value: any` → `this`

**不變量**：插入後 `tail` 指向新節點，新節點的 `next === null`

**基礎實作（無 tail 指標）**：

```javascript
append(value) {
  const node = new ListNode(value);

  if (!this.head) {
    this.head = node;
    return this;
  }

  // 必須遍歷找到最後一個節點
  let current = this.head;
  while (current.next) {
    current = current.next;
  }
  current.next = node;

  this.length++;
  return this;
}
```

**基礎版本的問題**：

- 每次 append 都要 O(n) 遍歷找尾巴
- 如果頻繁 append，效率極低

**優化策略**：維護 tail 指標

```javascript
append(value) {
  const node = new ListNode(value);

  if (!this.head) {
    this.head = this.tail = node;
  } else {
    this.tail.next = node;  // O(1) 直接存取尾部
    this.tail = node;
  }

  this.length++;
  return this;
}
```

**優化後複雜度**：O(1)

---

#### 4. **insertAt(index, value) — 中間插入**

**目的**：在指定位置插入新節點

**輸入/輸出**：`index: number, value: any` → `this | null`

**不變量**：原本在 index 位置的節點變成在 index + 1

**關鍵洞察**：插入本身是 O(1)，但「找到插入位置」需要 O(n)

```javascript
insertAt(index, value) {
  if (index < 0 || index > this.length) return null;
  if (index === 0) return this.prepend(value);
  if (index === this.length) return this.append(value);

  const node = new ListNode(value);

  // 找到 index - 1 位置的節點（前驅節點）
  const prev = this.get(index - 1);  // O(n)

  // 執行插入（O(1)）
  node.next = prev.next;
  prev.next = node;

  this.length++;
  return this;
}
```

**視覺化**：

```plaintext
Insert at index 2:

Before: head → [A] → [B] → [C] → [D] → null
               0      1      2      3

Step 1: Find prev (index 1)
        head → [A] → [B] → [C] → [D] → null
                      ↑
                    prev

Step 2: node.next = prev.next
        head → [A] → [B] → [C] → [D] → null
                           ↗
                        [X]

Step 3: prev.next = node
        head → [A] → [B] → [X] → [C] → [D] → null
               0      1      2      3      4
```

**複雜度**：O(n)（定位）+ O(1)（插入）= **O(n)**

---

#### 5. **removeAt(index) — 刪除指定位置**

**目的**：移除指定位置的節點

**輸入/輸出**：`index: number` → `Node | null`（被移除的節點）

**不變量**：原本在 index + 1 位置的節點變成在 index

**關鍵洞察**：需要找到「前驅節點」才能修改其 next 指標

```javascript
removeAt(index) {
  if (index < 0 || index >= this.length) return null;

  let removed;

  if (index === 0) {
    // 移除頭部：特殊處理
    removed = this.head;
    this.head = this.head.next;
    if (this.length === 1) this.tail = null;
  } else {
    // 找前驅節點
    const prev = this.get(index - 1);
    removed = prev.next;
    prev.next = removed.next;

    // 如果移除的是 tail，更新 tail
    if (index === this.length - 1) {
      this.tail = prev;
    }
  }

  this.length--;
  return removed;
}
```

**視覺化**：

```plaintext
Remove at index 2:

Before: head → [A] → [B] → [C] → [D] → null
               0      1      2      3

Step 1: Find prev (index 1)
        head → [A] → [B] → [C] → [D] → null
                      ↑      ↑
                    prev  removed

Step 2: prev.next = removed.next
        head → [A] → [B] ────→ [D] → null
                           [C] (斷開，等待 GC)
               0      1      2
```

**複雜度**：O(n)

---

#### 6. **reverse() — 反轉串列**

**目的**：將串列順序完全反轉

**輸入/輸出**：無 → `this`

**不變量**：原本的 head 變成 tail，原本的 tail 變成 head

**核心思想**：逐一將每個節點的 next 指向前一個節點

```javascript
reverse() {
  let prev = null;
  let current = this.head;

  // 交換 head 和 tail
  this.tail = this.head;

  while (current) {
    const next = current.next;  // 暫存下一個
    current.next = prev;        // 反轉指標
    prev = current;             // 前進 prev
    current = next;             // 前進 current
  }

  this.head = prev;
  return this;
}
```

**視覺化（逐步追蹤）**：

```plaintext
Initial:  null ← prev   current → [A] → [B] → [C] → null

Round 1:  null ← [A]    current → [B] → [C] → null
          ↑
         prev

Round 2:  null ← [A] ← [B]    current → [C] → null
                       ↑
                      prev

Round 3:  null ← [A] ← [B] ← [C]    current → null
                             ↑
                            prev

Final:    head = prev = [C]
          [C] → [B] → [A] → null
```

**複雜度**：O(n) 時間，O(1) 空間

---

### 核心操作複雜度總結

| 操作                   | 時間複雜度 | 空間複雜度 | 備註                       |
| ---------------------- | ---------- | ---------- | -------------------------- |
| **存取**               |            |            |                            |
| get(index)             | O(n)       | O(1)       | 必須從頭遍歷，**核心劣勢** |
| **插入**               |            |            |                            |
| prepend(value)         | **O(1)**   | O(1)       | 頭部插入，**核心優勢**     |
| append(value)          | **O(1)**   | O(1)       | 需維護 tail 指標           |
| insertAt(index, value) | O(n)       | O(1)       | 定位 O(n) + 插入 O(1)      |
| **刪除**               |            |            |                            |
| shift()                | **O(1)**   | O(1)       | 移除頭部                   |
| pop()                  | O(n)       | O(1)       | 需找到倒數第二個節點       |
| removeAt(index)        | O(n)       | O(1)       | 定位 O(n) + 刪除 O(1)      |
| **搜尋**               |            |            |                            |
| find(value)            | O(n)       | O(1)       | 線性搜尋                   |
| **其他**               |            |            |                            |
| reverse()              | O(n)       | O(1)       | 原地反轉                   |

**符號定義**：

- `n`：節點總數（length）

**結構本身的空間複雜度**：O(n)（每個節點需存 value + 1 個指標）

---

## ⭐ 抽象化翻譯器

### 識別核心抽象

> **這個工具的核心對象是**：獨立的節點（Node）
>
> **它管理的是這些對象之間的什麼關係**：單向的「後繼」關係（每個節點只知道它的下一個是誰）

### 建立映射維度

| 維度                 | 要回答的問題                   | 這個答案決定了什麼     |
| -------------------- | ------------------------------ | ---------------------- |
| **維度 1：資料特性** | 需要頻繁在哪個位置操作？       | 是否適合用 Linked List |
| **維度 2：方向性**   | 只需要往前走，還是需要往回走？ | Singly vs Doubly       |
| **維度 3：邊界處理** | head/tail 需要特殊處理嗎？     | 是否需要 dummy node    |
| **維度 4：指標操作** | 需要幾個指標？它們如何移動？   | 演算法設計的核心       |

### 實戰檢查表

```plaintext
題目：_______________

維度 1（資料特性）：操作主要在頭/尾/中間？_______________
維度 2（方向性）：需要往回走嗎？_______________
維度 3（邊界處理）：空串列/單節點需要特殊處理嗎？_______________
維度 4（指標操作）：需要 slow/fast？需要 prev/current/next？_______________

判斷流程：
├── 頻繁在頭尾操作 + 不需要隨機存取 → Linked List 適用
├── 需要往回走 → Doubly Linked List
├── 只需要往前 + 想節省空間 → Singly Linked List
└── 需要 O(1) 隨機存取 → Array 更適合
```

### 映射範例

**題目 A**：[206. Reverse Linked List](https://leetcode.com/problems/reverse-linked-list/)

| 維度   | 具體問題                   | 抽象映射                           |
| ------ | -------------------------- | ---------------------------------- |
| 維度 1 | 需要處理每個節點           | 需要遍歷整個串列                   |
| 維度 2 | 要把 next 反過來指         | 需要記住「前一個」→ 需要 prev 指標 |
| 維度 3 | 空串列/單節點直接返回      | 需要邊界檢查                       |
| 維度 4 | prev, current, next 三指標 | 標準反轉模式                       |

**從映射到實作的關鍵步驟**：

1. 初始化 `prev = null, current = head`
2. 每輪：保存 next → 反轉 current.next → 移動 prev, current
3. 返回 prev（新的 head）

---

**題目 B**：[141. Linked List Cycle](https://leetcode.com/problems/linked-list-cycle/)

| 維度   | 具體問題     | 抽象映射                             |
| ------ | ------------ | ------------------------------------ |
| 維度 1 | 判斷是否有環 | 需要某種方式偵測「重複到達同一節點」 |
| 維度 2 | 單向串列     | 只能往前走                           |
| 維度 3 | 空串列無環   | 邊界處理                             |
| 維度 4 | 快慢指標     | Floyd's Cycle Detection              |

**從映射到實作的關鍵步驟**：

1. slow 走一步，fast 走兩步
2. 如果有環，fast 必定追上 slow
3. 如果 fast 到達 null，則無環

---

## 🔍 觸發器（模式識別）

### 正向觸發器

| 層級         | 特徵                                        | 為什麼這個特徵指向 Linked List |
| ------------ | ------------------------------------------- | ------------------------------ |
| **關鍵字**   | "頭部/尾部插入刪除"、"反轉"、"合併兩個串列" | Linked List 的優勢操作         |
| **結構**     | 給定 head 節點、節點有 next 指標            | 題目本身就是 Linked List       |
| **目標**     | 不需要隨機存取、需要 O(1) 插入刪除          | 符合 Linked List 的 trade-off  |
| **操作模式** | 需要追蹤前後關係、需要快慢指標              | Linked List 經典技巧           |

**一句話觸發規則**：當題目給定 Linked List 結構，或需要頻繁在頭尾操作且不需要隨機存取時，使用 Linked List。

### 反向觸發器

| 陷阱情境                 | 為什麼不適用             | 更好的選擇         |
| ------------------------ | ------------------------ | ------------------ |
| 需要頻繁根據 index 存取  | O(n) 隨機存取太慢        | Array              |
| 需要往回遍歷             | Singly 無法往回          | Doubly Linked List |
| 需要二分搜尋             | 無法 O(1) 取中間元素     | Array              |
| 記憶體受限且元素數量固定 | 每個節點額外一個指標開銷 | Array              |

### 與類似工具的決策點

```plaintext
需要線性存儲一組資料
├── 需要 O(1) 隨機存取？
│   ├── 是 → Array
│   └── 否 → 繼續判斷
├── 需要頻繁在頭部操作？
│   ├── 是 → Linked List（prepend O(1)）
│   └── 否 → Array（shift 要 O(n)，但存取快）
├── 需要頻繁在中間插入刪除？
│   ├── 是 + 已有位置指標 → Linked List
│   └── 是 + 需要先找位置 → 考慮其他結構（如 BST）
└── 需要往回遍歷？
    ├── 是 → Doubly Linked List
    └── 否 → Singly Linked List
```

---

## ⚠️ 注意事項

### 常見陷阱

| 陷阱                         | 錯誤寫法                                              | 正確寫法                                      | 為什麼                                |
| ---------------------------- | ----------------------------------------------------- | --------------------------------------------- | ------------------------------------- |
| 忘記處理空串列               | `this.head.next`                                      | `if (!this.head) return` 先檢查               | null.next 會報錯                      |
| 忘記更新 tail                | 只更新 head                                           | 同時檢查是否需要更新 tail                     | 維護不變量                            |
| 移除頭部後忘記處理單節點情況 | `this.head = this.head.next`                          | 額外檢查 `if (length === 1) this.tail = null` | head 和 tail 可能指向同一節點         |
| 反轉時丟失下一個節點         | `current.next = prev` 後直接 `current = current.next` | 先 `const next = current.next` 保存           | 一旦反轉 next，原本的下一個就找不到了 |
| pop() 時間複雜度誤判         | 以為有 tail 就是 O(1)                                 | Singly 的 pop() 是 **O(n)**                   | 需要找到倒數第二個節點來更新 tail     |

### 常見變體

| 變體                     | 修改內容               | 適用場景                                      |
| ------------------------ | ---------------------- | --------------------------------------------- |
| **Doubly Linked List**   | 每個節點增加 prev 指標 | 需要雙向遍歷、O(1) pop()                      |
| **Circular Linked List** | tail.next = head       | 循環佇列、約瑟夫問題                          |
| **Dummy Head**           | 使用虛擬頭節點         | 簡化邊界處理（head 的插入刪除邏輯與中間一致） |
| **Sentinel Node**        | 頭尾各一個哨兵節點     | 避免 null 檢查                                |

### Dummy Head 模式詳解

**問題**：很多操作需要特殊處理 head（因為它沒有前驅節點）

**解法**：創建一個虛擬頭節點，讓真正的 head 也有「前驅」

```javascript
// 使用 Dummy Head 的刪除操作
removeAt(index) {
  const dummy = new ListNode(0);
  dummy.next = this.head;

  // 現在 index 0 的前驅就是 dummy
  const prev = this.getNodeWithDummy(dummy, index - 1);
  prev.next = prev.next.next;

  this.head = dummy.next;  // 更新真正的 head
}
```

**優點**：統一處理邏輯，減少 edge case
**適用題目**：203. Remove Linked List Elements、82. Remove Duplicates from Sorted List II

---

## 🎯 經典題型與觸發器總結

| 題型       | 觸發特徵                      | 核心技巧                 | 代表題目    |
| ---------- | ----------------------------- | ------------------------ | ----------- |
| **反轉類** | "reverse"、需要改變 next 指向 | prev/current/next 三指標 | 206, 92, 25 |
| **環檢測** | "cycle"、"circular"           | 快慢指標（Floyd's）      | 141, 142    |
| **找中點** | "middle"、需要分割串列        | 快慢指標（fast 走兩步）  | 876, 234    |
| **合併類** | "merge"、多個串列             | 雙指標比較 + dummy head  | 21, 23      |
| **刪除類** | "remove"、"delete"            | 找前驅 + dummy head      | 203, 19, 82 |
| **複製類** | "copy"、"clone"               | HashMap 存映射           | 138         |
